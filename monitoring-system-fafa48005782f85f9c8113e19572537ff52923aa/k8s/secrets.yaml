# ATENÇÃO: nunca commitar com valores reais!
# Use: kubectl create secret generic app-secrets --from-env-file=.env
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: monitoring
type: Opaque
stringData:
  database-url: "postgres://monitor:CHANGE_ME@db:5432/monitoring"
  db-password: "CHANGE_ME"
